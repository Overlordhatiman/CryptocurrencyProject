<Window x:Class="CryptocurrencyProject.Views.DetailsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="DetailsView" Height="450" Width="1000">
    <Grid Background="{StaticResource BackgroundBrush}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="10">
                <TextBlock Text="Currency Details" FontWeight="Bold" FontSize="16" Margin="0,0,0,10" Foreground="{StaticResource ForegroundBrush}"/>
                <TextBlock Text="ID:" FontWeight="Bold" Foreground="{StaticResource ForegroundBrush}"/>
                <TextBlock Text="{Binding SelectedCurrency.id}" Margin="0,0,0,5" Foreground="{StaticResource ForegroundBrush}"/>
                <TextBlock Text="Symbol:" FontWeight="Bold" Foreground="{StaticResource ForegroundBrush}"/>
                <TextBlock Text="{Binding SelectedCurrency.symbol}" Margin="0,0,0,5" Foreground="{StaticResource ForegroundBrush}"/>
                <TextBlock Text="Name:" FontWeight="Bold" Foreground="{StaticResource ForegroundBrush}"/>
                <TextBlock Text="{Binding SelectedCurrency.name}" Margin="0,0,0,5" Foreground="{StaticResource ForegroundBrush}"/>
                <TextBlock Text="Price (USD):" FontWeight="Bold" Foreground="{StaticResource ForegroundBrush}"/>
                <TextBlock Text="{Binding SelectedCurrency.priceUsd}" Margin="0,0,0,5" Foreground="{StaticResource ForegroundBrush}"/>
                <TextBlock Text="Volume (24Hr):" FontWeight="Bold" Foreground="{StaticResource ForegroundBrush}"/>
                <TextBlock Text="{Binding SelectedCurrency.volumeUsd24Hr}" Margin="0,0,0,5" Foreground="{StaticResource ForegroundBrush}"/>
                <TextBlock Text="Change (24Hr %):" FontWeight="Bold" Foreground="{StaticResource ForegroundBrush}"/>
                <TextBlock Text="{Binding SelectedCurrency.changePercent24Hr}" Margin="0,0,0,5" Foreground="{StaticResource ForegroundBrush}"/>
                <TextBlock Text="Explorer:" FontWeight="Bold" Foreground="{StaticResource ForegroundBrush}"/>
                <TextBlock>
                    <Hyperlink NavigateUri="{Binding SelectedCurrency.explorer}" RequestNavigate="Hyperlink_RequestNavigate">
                        <TextBlock Text="{Binding SelectedCurrency.explorer}" Foreground="{StaticResource ForegroundBrush}"/>
                    </Hyperlink>
                </TextBlock>
                <Button Content="Show Candlestick Chart" Command="{Binding ShowCandlestickChartCommand}" Margin="10" Background="{StaticResource ButtonBackgroundBrush}" Foreground="{StaticResource ButtonForegroundBrush}"/>

                <TextBlock Text="Markets" FontWeight="Bold" FontSize="16" Margin="10,0,0,10" Foreground="{StaticResource ForegroundBrush}"/>
                <ListBox ItemsSource="{Binding SelectedCurrency.markets}" BorderThickness="1" BorderBrush="Black" Background="{StaticResource BackgroundBrush}" Foreground="{StaticResource ForegroundBrush}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Exchange ID:" Grid.Column="0" Margin="5" Foreground="{StaticResource ForegroundBrush}"/>
                                <TextBlock Text="{Binding exchangeId}" Grid.Column="1" Margin="5" Foreground="{StaticResource ForegroundBrush}"/>
                                <TextBlock Text="Base Symbol:" Grid.Column="2" Margin="5" Foreground="{StaticResource ForegroundBrush}"/>
                                <TextBlock Text="{Binding baseSymbol}" Grid.Column="3" Margin="5" Foreground="{StaticResource ForegroundBrush}"/>
                                <TextBlock Text="Quote Symbol:" Grid.Column="4" Margin="5" Foreground="{StaticResource ForegroundBrush}"/>
                                <TextBlock Text="{Binding quoteSymbol}" Grid.Column="5" Margin="5" Foreground="{StaticResource ForegroundBrush}"/>
                                <TextBlock Text="Volume (USD):" Grid.Column="6" Margin="5" Foreground="{StaticResource ForegroundBrush}"/>
                                <TextBlock Text="{Binding volumeUsd24Hr}" Grid.Column="7" Margin="5" Foreground="{StaticResource ForegroundBrush}"/>
                                <TextBlock Text="Price (USD):" Grid.Column="8" Margin="5" Foreground="{StaticResource ForegroundBrush}"/>
                                <TextBlock Text="{Binding priceUsd}" Grid.Column="9" Margin="5" Foreground="{StaticResource ForegroundBrush}"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
